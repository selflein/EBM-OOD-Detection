trainer_config:
  max_epochs: 10
  min_epochs: 10
  gpus: "0"

checkpoint_config:
  monitor: val_loss
  mode: min

earlystop_config:
  monitor: val_loss
  mode: min
  patience: 10

dataset: Gaussian2D
ood_dataset: AnomalousGaussian2D
seed: 1
batch_size: 128

model_name: EnergyOOD
model_config:
  arch_name: fc
  arch_config:
    inp_dim: 2
    num_classes: 3
  learning_rate: 0.0001
  momentum: 0.5
  weight_decay: 0.
  m_in: -23
  m_out: -5
  score: "energy"
  checkpoint: ""
  epochs: 10
  dl_len: 500
